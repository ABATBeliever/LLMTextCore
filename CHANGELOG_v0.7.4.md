# LLMTextCore v0.7.4 変更点

前回のレビューで指摘した問題のうち、実害がある/低コストで直せるものを修正しました。
**通常利用(同期APIだけ、または非同期APIだけを使う単一スレッドの利用)では挙動は変わりません。**
API・DLLエクスポートのシグネチャは一切変更していないので、既存の呼び出し側コード
(HSPスクリプトなど)は再ビルドなしでそのまま動きます。

## 修正した問題

### 1. 同期API・非同期APIの同時実行によるデータ競合 (重大)
`llmtc_generate` / `llmtc_generate_to_buffer` が非同期生成の実行状態(`g_status`)を
一切見ていなかったため、`llmtc_generate_async` が動いている最中に同期APIを呼ぶと、
2つのスレッドが同じ `g_reply_buffer` / `g_ctx` に同時にアクセスし得ました。

→ `try_start_generation()` という排他ゲートを新設し、同期・非同期どちらの入口でも
必ずこれを通すようにしました。非同期生成が実行中、またはDONE/CANCELLEDの結果が
未取得の間は、同期APIは失敗するようになります(`llmtc_generate` はNULL、
`llmtc_generate_to_buffer` は負値を返す。理由は `llmtc_get_last_error_to_buffer` で確認可)。

同期APIだけを使っている場合、`g_status` は呼び出しの前後で常にIDLEに戻るため、
この分岐には入らず、これまでと同じ結果が得られます。

### 2. `llmtc_generate_async` の TOCTOU レース (重大)
「状態を読む」→「BUSYに書き込む」が2ステップに分かれていたため、複数スレッドから
ほぼ同時に呼ばれると両方が開始条件を通過してしまう可能性がありました。

→ 状態遷移を `std::atomic::compare_exchange_weak` によるCASループ (`try_start_generation`)
に置き換え、1つのアトミック操作にしました。返すエラーコード(`-1` / `-2` / `-4`)は
従来通りです。

### 3. `g_last_error` が生成のたびに際限なく蓄積する問題 (中)
以前は `llmtc_init` の時にしかクリアされず、セッション中に出たERRORログが全部
連結され続けていました。

→ `do_generate()` の冒頭(nullチェック後)で毎回クリアするようにしました。
また、万一大量のログが出ても際限なく肥大化しないよう、ログコールバック側にも
簡易的なサイズ上限(4KB)を追加しています。

これにより、生成が失敗したときに `llmtc_get_last_error_to_buffer` で得られる内容が
「今回の呼び出しに関するエラー」だけになり、以前より原因が分かりやすくなります
(成功時の挙動に変化はありません)。

### 4. `llmtc_free()` が生成完了までブロックする問題 (中)
`BUSY` 中に `llmtc_free()` を呼ぶと、`max_tokens` まで生成が終わるまで無期限に
ブロックしていました。

→ `llmtc_free()` の冒頭で `llmtc_cancel()` 相当の処理(キャンセルフラグを立てる)を
行ってから `join()` するようにしました。生成が行われていない時に呼んでも、
キャンセルフラグを立てるだけなので無害です。

## 今回あえて直していない項目(頻度が低いため)

- `llama_token_to_piece` が256バイトを超えるトークン片を返した場合に無言でスキップされる件
  (通常のモデル・語彙ではまず発生しません)
- `g_top_p` 等の生成パラメータ用グローバル変数が `std::atomic` になっていない件
  (生成中に `llmtc_set_generation_params` を呼ぶ、という稀なケースのみ影響)
- **同期API (`llmtc_generate` 系) の実行中に、別スレッドから `llmtc_free()` を呼ぶケース**
  同期APIは呼び出したスレッドをブロックするだけで、非同期のようにワーカースレッドを
  持たないため、`llmtc_free` 側からは「今まさに同期生成中かどうか」を検知できません。
  この用途を想定するなら、生成中フラグをアトミックに共有し `llmtc_free` 側でも
  待ち合わせる仕組みが別途必要になります。基本的には「同期APIの呼び出しが完了した
  スレッドと同じ場所で `llmtc_free` を呼ぶ」運用を守ってください。
